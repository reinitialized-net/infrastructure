{
    "nix.enableLanguageServer": true,
    "nix.serverPath": "nixd",
    "nix.serverSettings": {
        "nixd": {
            "formatting": {
                "command": ["nixfmt"],
            },
            "options": {
                "nixos": {
                    // "expr": "(builtins.getFlake \"${workspaceFolder}\").nixosConfigurations.standard.options"
                },
            }
        }
    },
    "nixEnvSelector.nixFile": "${workspaceFolder}/flake.nix",

    "chat.agent.maxRequests": 100,
}

