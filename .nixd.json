{
  "nixpkgs": {
    "expr": "import <nixpkgs> { }"
  },
  "options": {
    "nixos": {
      "expr": "let flake = builtins.getFlake \"git+file://${./.}\"; nixpkgs = flake.inputs.nixpkgsStable; system = builtins.currentSystem; in (import (nixpkgs + \"/nixos/lib/eval-config.nix\") { inherit system; modules = [ flake.nixosModules.default ]; }).options"
    }
  }
}
