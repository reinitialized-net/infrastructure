{
  "nixpkgs": {
    "expr": "import <nixpkgs> { }"
  },
  "options": {
    "nixos": {
      "expr": "let flake = builtins.getFlake \"git+file://${./.}\"; pkgs = import flake.inputs.nixpkgsStable { system = builtins.currentSystem; }; in (pkgs.lib.evalModules { modules = [ flake.nixosModules.default ]; }).options"
    }
  }
}
